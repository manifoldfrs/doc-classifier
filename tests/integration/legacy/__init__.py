"""tests.integration.legacy package

Contains tests that exercise the **deprecated** Flask routes now mounted under
`/legacy`.  These tests ensure backward compatibility for existing consumers
while the new FastAPI layer is introduced.
"""
